[tool.ruff]
line-length = 92
target-version = "py310"
extend-exclude = ["build"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
ignore = ["E402"]  # sys.path setup must precede imports in the tools scripts

[tool.pytest.ini_options]
testpaths = ["tests"]
